{
  "name": "clipflow-backend",
  "version": "1.0.0",
  "description": "Backend for ClipFlow",
  "main": "server.js",
  "scripts": {
    "start": "node server.js", // For production
    "dev": "nodemon server.js"  // For development (using nodemon)
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.2.10",
    "dotenv": "^16.0.3" // If you are using .env files for local development
  },
  "devDependencies": {
    "nodemon": "^2.0.22" // Only for development
  }
}
